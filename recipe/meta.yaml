{% set name = "safer" %}
{% set version = "4.12.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/safer-{{ version }}.tar.gz
  sha256: 8621b05038f4397af31100a49182fe45a7a7fcab6c8bca7eb9fb12a52d743528

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.7,<4.0
    - poetry-core
    - pip
  run:
    - black >=23.12.0,<24.0.0
    - python >=3.7,<4.0

test:
  imports:
    - safer
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/rec/safer
  summary: 🧿 A safer writer for files and streams 🧿
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - ocefpaf
